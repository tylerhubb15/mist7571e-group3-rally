RALLY — ELEVATOR PITCH
Tyler | Role 2 (AI Integration) + System Administrator
Target: 45–60 seconds
============================================================

ELEVATOR PITCH  (read this out loud — ~55 seconds)
----------------------------------------------------
Before anyone else on the team could write a single line
of feature code, the infrastructure had to exist. That
was my job.

I set up every external service the app depends on —
Google Cloud for the map and Google sign-in, Supabase
for the database and user authentication, and Netlify for
hosting and automated deployments. I also wrote the build
configuration that ties all of it together, and stored all
four API keys in Netlify's secure environment vault so
they never appear anywhere in the codebase.

Once the foundation was in place, I built the AI layer on
top of it. Rally has a coaching chat and a pre-match brief
that gives you three personalized tips on your opponent
before you play. What's worth pointing out is that the AI
call never goes through the browser — I wrote a serverless
function that acts as the intermediary, so the OpenAI key
is never exposed to the client side.

[PAUSE — check time — then hand off to next person]

